#ifndef channel_h_
#define channel_h_

// stores information about the sample in this channel
// each key stores a midi note / tuning & start,end pair - could potentially store other information in here as well
class Channel
{
  
};
#endif
